name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Get version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Install Squirrel CLI
        run: dotnet tool install -g csq

      - name: Publish app
        shell: bash
        run: |
          dotnet publish LucasScreentime/LucasScreentime.csproj \
            -c Release \
            -r win-x64 \
            --self-contained \
            -p:PublishSingleFile=true \
            -p:DebugType=none \
            -o publish/

      - name: Package with Squirrel
        shell: bash
        run: |
          csq pack \
            --packId "LucasScreentime" \
            --packVersion "${{ steps.version.outputs.VERSION }}" \
            --packDir "publish/" \
            --releaseDir "releases/" \
            --packTitle "Lucas Screentime" \
            --packAuthors "ScreenTracker"

      - name: Upload release artifacts
        uses: softprops/action-gh-release@v2
        with:
          name: "v${{ steps.version.outputs.VERSION }}"
          generate_release_notes: true
          files: releases/*
